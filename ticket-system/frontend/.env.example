# O frontend fala com o backend através do proxy do Vite (ver vite.config.js),
# por isso normalmente não é preciso configurar nada em desenvolvimento.
# Em produção, sirva o build estático atrás do mesmo domínio do backend
# (ou configure um proxy reverso para /api e /socket.io).
